# Agents

Agents are stateful subjects that observe signals, face constraints, and make
decisions.

Initial schemas:

- `firm.yaml`
- `investor.yaml`
- `bank.yaml`

Exchange mechanisms and external macro variables are not agents in v0.
